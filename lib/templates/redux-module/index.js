import { combineReducer } from 'redux';
import $(actionCamel)Reducer from './reducer.$(actionCamel)';


const reducer = combineReducer({
    $(actionCamel): $(actionCamel)Reducer,
});

export default reducer;