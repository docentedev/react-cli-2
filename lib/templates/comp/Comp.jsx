import React from 'react';
import './$(pascal).css';

/**
 * $(pascal)
 * @version v1.0.0
 * 
 * @param {*} props 
 */
const $(pascal) = (props) => {

    return (
        <div className="$(kebab)">$(pascal)!</div>
    );
};

export default $(pascal);